FROM node:18-alpine

WORKDIR /app

# Install frontend dependencies for local development
COPY frontend/package*.json ./
RUN npm install

# Copy frontend source for initial image state; bind mounts override in dev
COPY frontend/ ./

EXPOSE 3000

CMD ["npm", "start"]
